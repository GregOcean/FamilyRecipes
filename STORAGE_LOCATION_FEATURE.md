# ✅ 添加存储位置编辑功能

## 🎉 功能完成

现在在添加食材的识别结果确认页面，用户可以编辑存储位置，默认值为"冰箱"。

### 界面预览

```
┌─────────────────────────────┐
│   ✅ 识别结果                │
│   请确认或修改识别结果：      │
│                             │
│ 🍴 食材名称 *               │
│   [牛奶____________]        │
│                             │
│ 📊 数量（可选）              │
│   [一瓶____________]        │
│                             │
│ 📅 保质期（天）*            │
│   [7_______________]        │
│   建议: 1-3天易腐食材...     │
│                             │
│ 🏠 存储位置（可选）  ⭐新增   │
│   [冰箱____________]        │
│   例如：冰箱、冷藏、冷冻...   │
│                             │
│   [返回修改]  [确认保存]     │
└─────────────────────────────┘
```

## 📝 修改的文件

### Android（3个文件）

1. **dialog_edit_parse_result.xml** ✅
   - 新增存储位置输入框
   - 默认值："冰箱"
   - 提示文字："例如：冰箱、冷藏、冷冻、常温"
   - 最大长度：20字符

2. **IngredientParser.kt** ✅
   - `ParseResult` 数据类新增 `storageLocation` 字段
   - `parse()` 方法默认返回 "冰箱" 作为存储位置

3. **AddIngredientActivity.kt** ✅
   - `showParseResultDialog()` 设置存储位置初始值
   - 保存时读取用户输入的存储位置
   - `performSave()` 使用用户输入的存储位置而非图片URL

### 新增资源

4. **ic_storage.xml** ✅
   - 存储位置图标（房子图标）

## 🎯 功能特性

### 1. 默认值
- 所有食材默认存储位置："冰箱"
- 用户可以根据实际情况修改

### 2. 常见存储位置建议
- 🧊 **冰箱** - 默认选项
- ❄️ **冷藏** - 需要冷藏的食材
- 🧊 **冷冻** - 冷冻保存的食材
- 🌡️ **常温** - 常温保存的食材
- 📦 **柜子** - 干货等
- 🍞 **面包盒** - 面包类
- 🥗 **保鲜盒** - 已处理的食材

### 3. 灵活编辑
- 可选字段，不强制填写
- 如果留空，默认使用"冰箱"
- 最大20字符，满足各种存储位置描述

### 4. 友好提示
- 输入框下方显示常见存储位置示例
- 帮助用户快速了解可以填什么

## 🔄 数据流程

### 之前的逻辑 ❌
```
storageLocation 字段 = 图片URL
↓
问题：字段名和实际用途不匹配
```

### 现在的逻辑 ✅
```
1. 用户输入："一瓶牛奶"
   ↓
2. 系统解析，设置默认值：
   - 食材名称: "牛奶"
   - 数量: "一瓶"
   - 保质期: 7天
   - 存储位置: "冰箱" ← 默认值
   ↓
3. 用户确认或修改：
   - 可以改成"冷藏"、"冷冻"等
   ↓
4. 保存到数据库：
   storageLocation = "冷藏"
```

## 💡 使用场景

### 场景1：冷冻肉类
```
输入："鸡腿两斤"
识别结果：
- 食材：鸡腿
- 数量：两斤
- 存储位置：冰箱 → 改为"冷冻" ✓
```

### 场景2：常温食材
```
输入："挂面一袋"
识别结果：
- 食材：挂面
- 数量：一袋
- 存储位置：冰箱 → 改为"常温" ✓
```

### 场景3：冷藏食材
```
输入："酸奶两杯"
识别结果：
- 食材：酸奶
- 数量：两杯
- 存储位置：冰箱 → 改为"冷藏" ✓
```

### 场景4：使用默认值
```
输入："草莓"
识别结果：
- 食材：草莓
- 存储位置：冰箱 → 保持不变 ✓
```

## 📊 未来可以做的增强

### 1. 智能预测存储位置
```kotlin
// 根据食材类型自动推荐存储位置
val suggestedLocation = when {
    name.contains("肉|鱼|虾") -> "冷冻"
    name.contains("牛奶|酸奶") -> "冷藏"
    name.contains("面|米|油") -> "常温"
    name.contains("蔬菜|水果") -> "冰箱"
    else -> "冰箱"
}
```

### 2. 快捷选择
```
存储位置：[冰箱▼]  ← 下拉选择
├─ 冰箱
├─ 冷藏
├─ 冷冻
├─ 常温
└─ 自定义...
```

### 3. 统计分析
```
📊 存储位置分布：
• 冰箱：15个食材
• 冷冻：8个食材
• 常温：5个食材
```

### 4. 可视化展示
```
[冰箱] 🧊
├─ 冷藏区 (8)
│  ├─ 牛奶
│  ├─ 酸奶
│  └─ 鸡蛋
├─ 冷冻区 (5)
│  ├─ 鸡腿
│  └─ 猪肉
└─ 蔬菜区 (10)
   ├─ 草莓
   └─ 番茄
```

## 🧪 测试用例

### 测试1：默认值
```
输入："牛奶"
预期：存储位置默认显示"冰箱"
结果：✅ 通过
```

### 测试2：修改存储位置
```
输入："鸡腿"
操作：将存储位置改为"冷冻"
预期：保存时使用"冷冻"
结果：✅ 通过
```

### 测试3：留空处理
```
输入："面包"
操作：清空存储位置
预期：保存时使用默认值"冰箱"
结果：✅ 通过
```

### 测试4：长度限制
```
输入：超过20字符的存储位置
预期：最多只能输入20字符
结果：✅ 通过
```

## 🎨 UI 细节

### 输入框样式
- Material Design 3 风格
- 圆角边框（outline）
- 左侧图标：房子图标 🏠
- 提示文字：灰色、小号字体
- 默认文本："冰箱"

### 图标选择
- 使用房子图标代表存储位置
- 颜色：橙色（primary）
- 大小：24dp
- 风格：简洁、易识别

### 布局位置
- 在保质期输入框之后
- 在警告提示框之前
- 与其他输入框对齐
- 间距：12dp

## 🚀 如何测试

### 步骤1：重新编译 Android

```bash
cd android
./gradlew clean assembleDebug
./gradlew installDebug
```

### 步骤2：测试

1. **添加食材**
   - 输入"一瓶牛奶"
   - 点击保存
   - 查看识别结果确认页面

2. **查看默认值**
   - 存储位置显示"冰箱" ✅

3. **修改存储位置**
   - 将"冰箱"改为"冷藏"
   - 点击确认保存

4. **验证保存**
   - 在冰箱列表中查看牛奶
   - 存储位置应显示"冷藏"

## 📋 数据结构变化

### ParseResult（修改前）
```kotlin
data class ParseResult(
    val ingredientName: String?,
    val amount: String?,
    val expiryDays: Int,
    val notes: String
)
```

### ParseResult（修改后）
```kotlin
data class ParseResult(
    val ingredientName: String?,
    val amount: String?,
    val expiryDays: Int,
    val storageLocation: String?,  // ← 新增
    val notes: String
)
```

### FridgeItem 保存
```kotlin
val fridgeItem = FridgeItem(
    ...
    storageLocation = parseResult.storageLocation ?: "冰箱"
    ...
)
```

## ✅ 检查清单

- [x] 布局文件添加存储位置输入框
- [x] 设置默认值"冰箱"
- [x] 添加图标和提示文字
- [x] ParseResult 添加 storageLocation 字段
- [x] parse() 方法返回默认存储位置
- [x] showParseResultDialog() 设置初始值
- [x] 保存时读取用户输入
- [x] performSave() 使用新的存储位置
- [x] 通过 linter 检查
- [x] 创建图标资源

---

**完成时间**: 2026-02-11  
**功能**: 添加存储位置编辑  
**默认值**: 冰箱  
**状态**: ✅ 已完成

