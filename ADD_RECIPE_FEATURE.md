# 添加菜谱功能说明

## ✅ 已实现的功能

### 1. **点击"+"号跳转到添加菜谱页面**
- 点击底部导航栏中间的"+"按钮
- 会打开全新的添加菜谱页面（`EditRecipeActivity`）

### 2. **菜名输入**
- 顶部有独立的菜名输入框
- 必填字段，如果未填写会提示错误

### 3. **多图片上传**
- 支持选择多张图片
- 图片以3列网格形式展示
- 每张图片右上角有删除按钮
- 最后一个位置显示"+"按钮用于继续添加图片

### 4. **心得分享**
- 大文本输入框
- 提示文字："分享你的下厨心得：替代食材、调料搭配、火候窍门，或这道菜背后的生活小故事..."
- 支持多行输入

### 5. **外部链接功能** ⭐ 新增
- 文本框下方有"添加外部做法链接"按钮
- 点击后弹出对话框，可以输入：
  - 链接标题（可选）
  - 链接地址（必填）
- 支持添加多个外部链接
- 每个链接显示为卡片形式，右侧有删除按钮

### 6. **标签选择**
参考原界面保留了标签功能，分为4类：

#### 时段标签
- 早餐、午餐、晚餐、夜宵、下午茶、点心

#### 类型标签
- 汤、炒菜、面食、糕点、凉菜、主食

#### 主食材标签
- 牛肉、羊肉、鸡肉、猪肉、鱼、米、面、蔬菜

#### 特殊需求标签
- 无葱姜蒜、宝宝餐、低糖、低盐、素食

### 7. **去除微博同步功能** ✅
- 已按要求移除

## 📱 界面布局

```
┌─────────────────────────────┐
│ [X]  添加菜谱         [发布] │  ← 顶部工具栏
├─────────────────────────────┤
│ [菜名输入框]                 │  ← 菜名
├─────────────────────────────┤
│ [图] [图] [+]                │  ← 图片网格（3列）
│ [图] [图] [ ]                │
├─────────────────────────────┤
│ [心得分享大文本框]            │  ← 心得内容
├─────────────────────────────┤
│ 外部做法链接                 │
│ ┌─ 链接1 ────────────── [X]┐│
│ │ 标题                      ││
│ │ https://...              ││
│ └──────────────────────────┘│
│ [+ 添加外部做法链接]          │  ← 添加链接按钮
├─────────────────────────────┤
│ # 标签                       │
│ 时段: [早餐] [午餐] [晚餐]... │
│ 类型: [汤] [炒菜] [面食]...   │
│ 主食材: [牛肉] [羊肉]...      │
│ 特殊需求: [无葱姜蒜]...       │
└─────────────────────────────┘
```

## 🔧 技术实现

### 新增文件

#### Kotlin文件
- `EditRecipeActivity.kt` - 添加菜谱页面主逻辑
- `ImageGridAdapter` - 图片网格适配器
- `ExternalLinkAdapter` - 外部链接列表适配器

#### 布局文件
- `activity_edit_recipe.xml` - 主布局
- `item_image_preview.xml` - 图片预览项
- `item_add_image.xml` - 添加图片按钮项
- `item_external_link.xml` - 外部链接项
- `dialog_add_link.xml` - 添加链接对话框

#### Drawable资源
- `ic_close.xml` - 关闭图标
- `circle_background_dark.xml` - 深色圆形背景（用于删除按钮）

### 修改文件
- `MainActivity.kt` - 添加了点击"+"号跳转到EditRecipeActivity的逻辑

## 🚀 使用方式

1. **打开应用**
2. **点击底部导航栏中间的"+"号**
3. **填写菜谱信息：**
   - 输入菜名（必填）
   - 选择至少一张图片（必填）
   - 填写心得分享（可选）
   - 添加外部链接（可选）
   - 选择标签（可选）
4. **点击右上角"发布"按钮**

## 📝 待完成功能

目前点击"发布"按钮会提示"发布功能开发中..."，后续需要：

1. 实现图片上传到服务器的逻辑
2. 调用后端API保存菜谱数据
3. 保存成功后返回到菜谱列表并刷新
4. 添加加载状态提示

## 🎨 UI特性

- ✅ 使用Material Design 3设计规范
- ✅ 支持滚动浏览所有内容
- ✅ Chip标签支持多选
- ✅ 图片网格自适应布局
- ✅ 外部链接列表动态添加/删除
- ✅ 输入验证和错误提示
- ✅ 符合Android开发最佳实践

