# 食材识别结果确认页面更新说明

## 更新内容

### 1. 按钮文字修改
- **原"修改"按钮** → **"返回修改"按钮**
  - 用户点击后会关闭对话框，返回到输入页面重新编辑原始输入
  - 保存按钮恢复可用状态

### 2. 新增真正的识别结果修改功能 ✨
现在用户可以直接在确认对话框中修改识别结果，包括：

#### 可修改的字段：
1. **食材名称** （必填）
   - 最大长度：50字符
   - 图标：餐具图标
   - 用途：防止自动识别错误时可以手动修正

2. **数量** （可选）
   - 最大长度：20字符
   - 图标：柱状图图标
   - 支持任意文本格式（如："2瓶"、"500克"、"一袋"等）

3. **保质期** （必填，单位：天）
   - 输入类型：纯数字
   - 验证规则：
     - 必须是大于0的整数
     - 不能超过3650天（10年）
   - 图标：日历图标
   - 智能提示：显示常见保质期建议

#### 智能警告功能：
- 当保质期 ≤ 3天时，自动显示警告提示框
- 警告文字会动态显示剩余天数
- 实时响应：修改保质期时即时更新警告状态

### 3. 新增文件

#### 布局文件：
- `dialog_edit_parse_result.xml` - 可编辑的识别结果对话框布局
  - 使用Material Design 3风格
  - 包含3个输入框和底部按钮栏
  - 带图标的文本输入框，界面美观友好

#### 资源文件：
- `warning_background.xml` - 警告提示框的背景drawable
  - 橙黄色背景 (#FFF3E0)
  - 橙色边框 (#FFB74D)
  - 8dp圆角

#### 图标资源：
- `ic_food.xml` - 食材图标（餐具）
- `ic_numbers.xml` - 数量图标（柱状图）
- `ic_calendar.xml` - 保质期图标（日历）

### 4. 代码改动

#### AddIngredientActivity.kt
**新增导入：**
```kotlin
import android.text.Editable
import android.text.TextWatcher
import android.view.LayoutInflater
import android.view.View
import com.familyrecipes.android.databinding.DialogEditParseResultBinding
```

**重写方法 `showParseResultDialog()`：**
- 使用自定义对话框布局替代简单的AlertDialog
- 添加ViewBinding支持
- 实现实时输入验证
- 添加TextWatcher监听保质期变化
- 完整的输入验证逻辑
- 支持用户修改后重新保存

## 用户体验提升

### 使用流程：
1. 用户输入食材信息后点击"保存"
2. 系统自动识别并弹出确认对话框
3. **新功能**：对话框中显示可编辑的输入框
4. 用户可以直接修改任何字段：
   - 修正识别错误的食材名称
   - 调整数量信息
   - 修改保质期天数
5. 两种选择：
   - **确认保存**：使用当前（可能已修改的）信息保存
   - **返回修改**：关闭对话框，返回重新输入原始文本

### 优点：
- ✅ 更灵活：不需要重新输入就能快速调整
- ✅ 更准确：可以直接修正AI识别的小错误
- ✅ 更快速：小改动不需要返回重新输入整段文字
- ✅ 更友好：实时警告提示，帮助用户注意快过期的食材
- ✅ 更安全：完整的输入验证，防止无效数据

## 技术细节

### 输入验证规则：
```kotlin
// 食材名称
- 不能为空
- 自动trim首尾空格

// 数量
- 可以为空（会设置为null）
- 支持任意文本

// 保质期
- 必须填写
- 必须是整数
- 必须 > 0
- 必须 ≤ 3650（10年）
```

### 警告触发条件：
```kotlin
if (days in 1..3) {
    // 显示警告
}
```

## 测试建议

1. **基本功能测试**：
   - 输入"鸡蛋"，确认识别结果，修改保质期为2天
   - 输入"牛奶一瓶7天过期"，修改食材名称为"纯牛奶"
   - 输入"草莓"，修改数量为"500克"

2. **边界测试**：
   - 保质期输入0、负数、超大数字
   - 食材名称清空后尝试保存
   - 保质期输入1、3、4测试警告显示/隐藏

3. **交互测试**：
   - 点击"返回修改"确认回到输入页面
   - 多次修改保质期观察警告实时更新
   - 修改各字段后点击"确认保存"验证保存成功

## 兼容性
- 完全兼容现有代码
- 不影响原有的识别逻辑
- 保持原有的保存流程
- Material Design 3 组件向下兼容

